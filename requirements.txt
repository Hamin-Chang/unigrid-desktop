# UNIGRID Desktop — 필요한 파이썬 패키지
# 파이썬 3.12 에서 만들었고 3.12 로 시험한다.
#
#   python3.12 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
#
# 계산 엔진(engine/)은 MATLAB Runtime 이 따로 있어야 돈다 — pip 로 안 깔린다.

# 🚨 PySide6 는 6.9.1 로 고정한다 (2026-08-19 확정, 맥·윈도우 같은 판).
#    6.11.1 은 윈도우에서 `import PySide6.QtCore` 자체가 실패한다 —
#    Qt6Core.dll 이 ICU 함수(UCNV_TO_U_CALLBACK_SUBSTITUTE)를 찾는데 wheel 에
#    ICU DLL 이 없어 System32 의 icuuc.dll 이 잡히고, 거기엔 그 함수가 없다.
#    앱이 쓰는 Qt 이름 59 개가 6.9.1 에 전부 있어 맥도 6.9.1 로 내렸다.
#    ⚠️ 올릴 때는 두 컴퓨터에서 같이 올린다 — 한쪽만 올리면 판이 갈린다.
PySide6==6.9.1

# 🚨 pandas·numpy 도 판을 박는다 (2026-08-20 — 같은 까닭).
#    2026-08-20 윈도우 진단에서 **맥 pandas 2.3.3 / 윈도우 pandas 3.0.5** 로
#    갈려 있는 것이 드러났다. 3.0 은 메이저 판이 다르고(`stack()` 동작이 바뀌었다),
#    `load_case` 가 엑셀을 읽을 때 그걸 쓴다. 회귀 지문 21건은 전부 **맥 2.3.3**
#    에서 만든 것이라, 기준을 그쪽으로 맞춘다.
#    ⚠️ 올릴 때는 두 컴퓨터에서 같이 올리고, 올린 뒤 회귀를 다시 돌린다.
pandas==2.3.3
numpy==2.4.6
openpyxl
